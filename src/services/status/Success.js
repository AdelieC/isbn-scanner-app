export default class Success {
    constructor(message, buttonText, icon, link, callback) {
        this.message = message;
        this.buttonText = buttonText;
        this.icon = icon;
        this.link = link;
        this.callback = callback;
    }
}
